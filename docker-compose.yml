services:
  db-webdinamis:
    image: mariadb:lts
    environment:
      MARIADB_ROOT_PASSWORD: "password!123"
      MARIADB_DATABASE: dbcommerce_2388010022
      MARIADB_USER: userwebdinamis_2388010022
      MARIADB_PASSWORD: "@Gama17026"
    volumes:
      - db_data:/var/lib/mysql
      - ./db_data/dbcommerce_2388010022.sql:/docker-entrypoint-initdb.d/dbcommerce_2388010022.sql:ro
    restart: always

  container-statis:
    image: gamine1/himafor_2388010022:latest
    ports:
      - "80:80"
    restart: always

  container-dinamis:
    image: gamine1/web-dinamis:latest
    ports:
      - "3000:3000"
    restart: always
    environment:
      DB_USER: userwebdinamis_2388010022
      DB_PASSWORD: "@Gama17026"
      DB_NAME: dbcommerce_2388010022
      DB_HOST: db-webdinamis
      DB_PORT: 3306
      NEXTAUTH_SECRET: ini-kata-kata-bebas-saja-32-karakter
      NEXTAUTH_URL: http://3.231.60.174:3000
    depends_on:
      - db-webdinamis

volumes:
  db_data: {}